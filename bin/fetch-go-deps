#!/bin/bash

# export GOPATH=~/go

# /usr/local/bin/grep -Poh "(github.com|golang.org|gopkg.in)/.*$" $(find go-application/{libraries,commands} -type f\
# 	| grep \\.go$)\
# 	| grep -v walterjwhite\
# 	| sed -e "s/\"//"\
# 	| sort -u\
# 	| xargs -P 1 -I _PACKAGE go get -u _PACKAGE

# requires modules to be setup
go mod download